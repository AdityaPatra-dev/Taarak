name: taarak_backend
description: >
  Local backend stub for validating TAARAK's auth + sync client contract
  end to end. Not a production service — see README.md before using this
  for anything beyond local dev/demo.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: ^3.9.0

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4

dev_dependencies:
  test: ^1.25.0
  http: ^1.2.0
  lints: ^5.0.0
